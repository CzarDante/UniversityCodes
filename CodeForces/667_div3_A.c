#inclde <stdio.h>

in main()
{
	int a,b;
	scanf("%d %d",&a, &b);
	if(a>b)
		distancia = a-b;
		vezes += distancia/10
		if(distancia%10 != 0){
			vezes+=1
			printf("%d",vezes);
		}
		else{
			printf("%d",vezes);
			}
	else{
		distancia = b-a;
		vezes += distancia/10
		if(distancia%10 != 0){
			vezes+=1
			printf("%d",vezes);
		}
		else{
			printf("%d",vezes);
			}
	}
}